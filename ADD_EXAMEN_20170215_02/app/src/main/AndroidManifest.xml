<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="masimeon.add_03c.florida.add_act03c">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EstudiantesActivity" />
        <activity android:name=".ProfesoresActivity" />
        <activity android:name=".EstNuevoActivity" />
        <activity android:name=".ProfNuevoActivity" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".ExamenActivity"></activity>
    </application>

</manifest>